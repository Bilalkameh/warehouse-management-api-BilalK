# Local demonstration values only. Do not reuse these credentials in production.
POSTGRES_USER=warehouse
POSTGRES_PASSWORD=warehouse
WAREHOUSE_DB=WarehouseDb
NOTIFICATIONS_DB=WarehouseNotificationsDb

MINIO_ROOT_USER=warehouse
MINIO_ROOT_PASSWORD=warehouse123
MINIO_BUCKET_NAME=warehouse-assets

RABBITMQ_DEFAULT_USER=warehouse
RABBITMQ_DEFAULT_PASS=warehouse

# Firebase is external. The containers only need the project ID to validate JWTs.
FIREBASE_PROJECT_ID=warehouse-management-apid